# devenv state
.devenv
.direnv
.pre-commit-config.yaml
.prettierignore

# local daemon config: the dev daemon resolves
# $MSKSD_CONFIG_DIR/msksd.yaml at the repo root (generated as the
# commented template on first start) — edits are personal. Anchored
# to the root so a tracked fixture of the same name deeper in the
# tree stays visible to git.
/msksd.yaml

# python
__pycache__/
*.py[cod]
.venv/

# test artifacts
.coverage
.coverage.*
.pytest_cache/
.testmondata

# nix build outputs
result

# rust (console-helper crate)
src/console-helper/target/
src/console-helper/.rustc_info.json
